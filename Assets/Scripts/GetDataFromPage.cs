using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class GetDataFromPage : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
    public void getData(string par)
    {
        // Your custom logic here using the parameters
        Debug.Log("Custom function called from HTML! Parameter1: "+par);
    }
}
